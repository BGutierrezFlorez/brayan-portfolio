import { ArrowUpRight, Mail } from "lucide-react";
import { profileLinks } from "@/data/experience";

export function Contact() {
  return <section id="contacto" className="contact-section"><div className="container flex max-w-3xl flex-col items-center text-center"><span className="eyebrow">07 / Contacto</span><h2 className="heading-display mt-4 text-4xl sm:text-5xl">¿Hablamos?</h2><p className="mt-5 max-w-lg leading-7 text-slate-300">Estoy abierto a oportunidades laborales, proyectos y colaboraciones.</p>{profileLinks.email ? <a className="button-primary mt-8" href={`mailto:${profileLinks.email}`}><Mail size={17} /> Contactarme</a> : <span className="button-primary mt-8 cursor-not-allowed opacity-60" title="Email pendiente de configurar"><Mail size={17} /> Contactarme</span>}<div className="mt-12 flex flex-wrap justify-center gap-5 border-t border-line/60 pt-7 text-sm text-slate-400">{profileLinks.email ? <a className="hover:text-brand" href={`mailto:${profileLinks.email}`}>{profileLinks.email}</a> : <span className="text-slate-500">Email pendiente de configurar</span>}<span className="text-slate-700">·</span>{profileLinks.github ? <a className="inline-flex items-center gap-1 hover:text-brand" href={profileLinks.github}>GitHub <ArrowUpRight size={13} /></a> : <span>GitHub pendiente</span>}<span className="text-slate-700">·</span>{profileLinks.linkedin ? <a className="inline-flex items-center gap-1 hover:text-brand" href={profileLinks.linkedin}>LinkedIn <ArrowUpRight size={13} /></a> : <span>LinkedIn pendiente</span>}</div></div></section>;
}
